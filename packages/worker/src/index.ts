// @neo/worker — background processing pipeline (Phase 2)
//
// This package will contain:
// - RSS feed fetcher (tiered: T1/4h, T2/8h, T3/daily)
// - Article processor (Gemini Flash summarization + embedding)
// - Graph triage (vector similarity + gap detection)
// - Research pipeline (Perplexity deep research)

export {};
